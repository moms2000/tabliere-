services:
  - type: web
    name: tabliereci-backend
    runtime: node
    rootDir: .
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4000
      - key: DATABASE_URL
        fromDatabase:
          name: tabliereci-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true
      - key: JWT_EXPIRES_IN
        value: 15m
      - key: JWT_REFRESH_IN
        value: 30d
      - key: APP_URL
        sync: false       # à remplir manuellement après déploiement
      - key: FRONTEND_URL
        sync: false       # URL Vercel, ex: https://tabliereci.vercel.app
      - key: REDIS_URL
        sync: false       # URL Upstash Redis, à remplir manuellement
      # Paiements — à remplir dans le dashboard Render
      - key: ORANGE_MONEY_API_KEY
        sync: false
      - key: ORANGE_MONEY_API_SECRET
        sync: false
      - key: MTN_MOMO_API_KEY
        sync: false
      - key: MTN_MOMO_USER_ID
        sync: false
      - key: MTN_MOMO_API_SECRET
        sync: false
      - key: WAVE_API_KEY
        sync: false
      # WhatsApp
      - key: WHATSAPP_TOKEN
        sync: false
      - key: WHATSAPP_PHONE_ID
        sync: false

databases:
  - name: tabliereci-db
    databaseName: tabliereci
    user: tabliereci
    plan: free
